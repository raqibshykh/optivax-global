# Production — mock server active until real backend is wired up.
# To switch to a real backend: set VITE_USE_MOCK_SERVER=false and VITE_API_URL=https://api.optivax.com
VITE_USE_MOCK_SERVER=true
VITE_API_URL=